상속
-extends
class A extends class B{}
- 부모 (기반)=> 자식(파생)
- 코드의 재사용
- 클래스에서 상속 단일 상속
- 인터페이스에서 상속: 다중상속이 가능

추상 메소드 : 메소드의 선언부만 있는 형태
	     메소드의 구현부가 없는 형태
인터페이스 : 추상메소드와 클래스 상수을 가짐

객체 멤버 메소드에서 클래스 맴버 변수를 사용 할 수 있다??
Yes
클래스 멤버 변수는 클래스이름을 통해 사용가능
=>객체 생성 없이 사용가능
=>객체가 생성되고 나서도 사용가능//원래 객체없이 사용이가능하기때문에

클래스 멤버 메소드에서 객체 멤버 변수를 사용 할수 있다??
NO
객체 멤버 변수는 객체를 생성하고 사용가능
클래스 멤버 메소드는 객체 생성 없이 사용이 가능한데
클래스 멤버 메소드 안에 객체 멤버 변수가 있다면 
객체를 안만든 상태에서 객체멤버 변수를 사용하려 하기 때문에
에러가난다.


try{						416p
	예외상황이 발생할 수 있는코드;
}catch{예외클래스){

}
일반적으로 예외처리가 안된 상태에서 예외가 발생하면 프로그램이 중단되는데
예외처리를 하면 예외가 발생해도 프로그램을 중단하지않고
다음 코드를 실행할 수있다.

catch는 if문처럼 순서대로 위에서 아래로 실행한다.

split 
- 문자열 => 배열 ,토큰을 기준으로
-"123,456,789"=> "," "123,"456"789"

join
- 배열 =>각 배열의 값 사이에 토큰을 추가하여 하나의
           문자열로
-"123","456","789" => "," =>"123,456,789"





















